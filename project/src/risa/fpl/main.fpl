use{
  risa.fpl.tokenizer
  std.io.file
  std.backend
}
ContextData context
main{
  if contextSave context; == 0{
    puts "first run"
  }{
    puts "context restored"
    exit 0
  }
  restore
}
fn void restore{
  contextRestore context
}
fn void placeholder{
 try{
         Exception ex "test"
         ex throw
       }catch Exception ex{
         puts "caught"
       }
}