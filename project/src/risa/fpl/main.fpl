use{
  risa.fpl.tokenizer
  std.io.file
  std.io.base
  std.collection
  std.backend
}
main{
 FileReader reader "test.fpl"
 Tokenizer tokenizer reader
 while tokenizer hasNext{
    tokenizer next
 }
 tokenizer close
}