use{
  risa.fpl.tokenizer
  std.io.file
  std.io.base
  std.collection
  std.backend
}
main{
 FileReader reader "project/src/risa/fpl/fpl.fpl"
 Tokenizer tokenizer reader
 while tokenizer hasNext{
    tokenizer next
 }
 tokenizer close
}