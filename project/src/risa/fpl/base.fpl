use std.io.file std.io.base
final{
 class CompilerException Exception{
  private String file
  this String message:message
  this int line int tokenNum String message:line toString;  + ":"; + tokenNum toString; + " "; + message
  fn void setFile String file = this file = file
  override fn String getMessage = file + ":" + message
 }
class IRWriter{
   const DataWriter writer
   this FileWriter writer{
     this writer = DataWriter:new writer
   }
   fn void close = writer close
 }
}