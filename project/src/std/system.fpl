use std.backend std.io.base

fn void exit int exitCode{
 currentThread freeEHentries
 exit exitCode
}
fn int execute String cmd{
  var ccmd cmd toCString
  var exitCode execute ccmd
  ccmd free
  if exitCode == -1{
    IOException:new "invalid command";throw
  }
  return exitCode
}