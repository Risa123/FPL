use std.backend std.io.base

fn void exit int exitCode{
 currentThread freeEHentries
 exit exitCode
}
fn int execute String cmd{
  var exitCode execute cmd toCString
  if exitCode == -1{
    IOException ex "invalid command"
    ex throw
  }
  return exitCode
}